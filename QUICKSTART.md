# 快速启动指南

## 文章导入功能使用说明

本系统使用本地 Puppeteer 服务器来获取网页内容，支持所有类型的网站（静态、动态、SPA）。

### 启动步骤

#### 1. 启动 Puppeteer 服务器

在终端中运行：

```bash
npm run puppeteer-server
```

您将看到类似以下输出：

```
Puppeteer 服务器运行在 http://localhost:3001
健康检查: http://localhost:3001/health
```

**重要**：保持此终端窗口打开，服务器需要持续运行。

#### 2. 启动前端应用

在另一个终端窗口中启动前端应用（按照项目的正常启动流程）。

#### 3. 使用文章导入功能

1. 在应用中找到"导入文章"按钮
2. 输入文章 URL
3. 点击"导入"
4. 等待 Puppeteer 服务器获取内容（通常需要 5-15 秒）
5. 系统会自动提取文章内容并显示

### 故障排查

#### 问题：提示"无法连接到 Puppeteer 服务器"

**解决方案：**
1. 确保 Puppeteer 服务器正在运行
2. 检查终端是否显示 `Puppeteer 服务器运行在 http://localhost:3001`
3. 如果服务器未运行，执行 `npm run puppeteer-server`

#### 问题：导入速度很慢

**原因：**
- Puppeteer 需要启动完整的 Chrome 浏览器
- 需要等待网页完全加载和 JavaScript 执行

**正常情况：**
- 静态网站：5-10 秒
- 动态网站（SPA）：10-20 秒

#### 问题：某些网站无法导入

**可能原因：**
1. 网站有反爬虫机制
2. 网站需要登录才能访问
3. 网站加载时间超过 30 秒（超时）

**解决方案：**
- 尝试其他文章链接
- 查看 Puppeteer 服务器终端的错误日志

### 技术说明

#### 为什么需要本地服务器？

- **Puppeteer 限制**：Puppeteer 只能在 Node.js 环境运行，无法在浏览器中使用
- **避免跨域**：服务端请求不受浏览器 CORS 限制
- **完整渲染**：Puppeteer 启动真实浏览器，完整执行 JavaScript

#### 架构图

```
前端 (React)
    ↓ HTTP 请求
Puppeteer 服务器 (Node.js + Express)
    ↓ 启动 Chrome 浏览器
目标网站
```

### 环境变量

在 `.env` 文件中配置：

```env
VITE_PUPPETEER_SERVER_URL=http://localhost:3001
```

如果您修改了服务器端口，请同步更新此配置。

### 日志查看

#### Puppeteer 服务器日志

在运行 `npm run puppeteer-server` 的终端中查看：
- 请求日志
- 浏览器启动日志
- 错误信息

#### 前端日志

在浏览器开发者工具的 Console 中查看：
- 文章获取进度
- 内容处理日志
- 错误提示

### 性能优化建议

1. **保持服务器运行**：避免频繁启动和关闭
2. **批量导入**：一次导入多篇文章时，服务器会复用浏览器实例
3. **网络环境**：确保网络连接稳定

### 安全提示

- Puppeteer 服务器仅监听 localhost，外部无法访问
- 不要在生产环境中使用默认配置
- 定期更新 Puppeteer 版本以获取安全补丁
