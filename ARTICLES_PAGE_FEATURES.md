# 文章管理页面 - 功能说明

## 设计理念

模仿 Apple 无边记（Freeform）的布局设计，采用 Jobs 设计哲学：
- 极致简约的黑白配色
- 清晰的信息层级
- 流畅的交互体验
- 完美的细节处理

## 页面布局

### 左侧导航栏（宽度：224px）
```
┌─────────────────┐
│  [新建文章]     │  ← 主操作按钮
│                 │
│  所有文章  ●    │  ← 当前选中
│  最近阅读       │
│                 │
└─────────────────┘
```

### 右侧主内容区
```
┌──────────────────────────────────────────┐
│ [编辑] [删除(n)]  [列表][卡片] [搜索框] │  ← 工具栏
├──────────────────────────────────────────┤
│                                          │
│  文章列表/卡片视图                        │
│                                          │
│                                          │
└──────────────────────────────────────────┘
```

## 核心功能

### 1. 视图切换
- **列表视图**：紧凑显示，适合快速浏览
  - 显示标题、作者、字数、时间
  - 悬停显示查看按钮
  - 支持快速点击阅读

- **卡片视图**：卡片式布局，视觉更丰富
  - 2-3列网格布局（响应式）
  - 显示完整信息
  - 悬停效果更明显

### 2. 搜索功能
- 实时搜索过滤
- 支持标题和作者搜索
- 搜索结果即时显示
- 无结果时显示提示

### 3. 编辑模式
- 点击"编辑"按钮进入编辑模式
- 显示选择框，支持多选
- 显示删除按钮和选中数量
- 点击"完成"退出编辑模式

### 4. 文章操作
- **查看**：点击文章跳转到阅读页面
- **删除**：编辑模式下批量删除
- **新建**：点击新建按钮创建文章（待实现）

## 交互细节

### 按钮设计
- 主操作按钮：黑底白字，圆角
- 次要按钮：透明背景，悬停变灰
- 视图切换：灰色背景，选中时白色

### 卡片/列表项
- 边框：极细边框（border-border/50）
- 悬停：边框加深，背景变化
- 选中：背景高亮
- 过渡：0.3s 流畅动画

### 搜索框
- 灰色背景，无边框
- 左侧搜索图标
- 聚焦时显示细边框
- 占位符透明度 60%

## 响应式设计

### 桌面端（≥1280px）
- 卡片视图：3列网格
- 搜索框：256px 宽度
- 完整显示所有功能

### 笔记本（<1280px）
- 卡片视图：2列网格
- 搜索框：保持宽度
- 布局自适应

## 技术实现

### 状态管理
```typescript
- articles: Article[]           // 所有文章
- filteredArticles: Article[]   // 过滤后的文章
- viewMode: 'list' | 'grid'     // 视图模式
- searchQuery: string           // 搜索关键词
- isEditMode: boolean           // 编辑模式
- selectedArticles: Set<number> // 选中的文章ID
```

### 路由配置
```
/                  → ArticlesPage（文章管理）
/read/:id          → ReadingPage（阅读页面）
/articles/new      → 新建文章（待实现）
```

### API 调用
```typescript
articlesApi.getArticles()       // 获取文章列表
articlesApi.getArticle(id)      // 获取单篇文章
// 待实现：
// articlesApi.deleteArticle(id)
// articlesApi.createArticle(data)
// articlesApi.updateArticle(id, data)
```

## 待实现功能

### 1. 新建文章
- 创建文章编辑器页面
- 支持标题、内容、作者等字段
- 自动保存草稿
- 发布文章

### 2. 编辑文章
- 点击编辑按钮进入编辑模式
- 修改文章内容
- 保存更新

### 3. 批量删除
- 实现批量删除 API
- 确认对话框
- 删除后刷新列表

### 4. 分类管理
- 添加文章分类
- 左侧导航显示分类
- 按分类筛选

### 5. 收藏功能
- 收藏文章
- 显示收藏列表
- 快速访问

### 6. 排序功能
- 按时间排序
- 按标题排序
- 按字数排序

## 设计亮点

### 1. 极简美学
- 纯黑白配色
- 去除多余装饰
- 专注内容本身

### 2. 流畅交互
- 即时反馈
- 平滑过渡
- 自然动画

### 3. 信息层级
- 清晰的视觉层次
- 合理的间距
- 易于扫描

### 4. 细节完美
- 像素级对齐
- 统一的圆角
- 精致的阴影

## 使用场景

1. **浏览文章**：快速查看所有文章，选择感兴趣的内容
2. **搜索文章**：通过关键词快速找到目标文章
3. **管理文章**：批量删除不需要的文章
4. **切换视图**：根据需求选择列表或卡片视图
5. **创建内容**：点击新建按钮开始写作

## 性能优化

- 文章列表虚拟滚动（大量文章时）
- 搜索防抖（300ms）
- 图片懒加载
- 组件按需加载

## 可访问性

- 键盘导航支持
- 屏幕阅读器友好
- 高对比度模式
- 焦点状态清晰
